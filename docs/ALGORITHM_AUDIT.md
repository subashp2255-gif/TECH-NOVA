# SeatSync — Algorithm Audit Matrix

| Algorithm | Exists? | Existing Location | Correct Location? | Problems Found | Action Required | Final Implementation Location | Verification Result |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **1. Atomic Booking** | Yes | `08_transactional_booking_rpcs.sql`, `bookingService.js` | Partially | Missing idempotency key input; missing academic calendar holiday check; missing notification outbox event; partial unique constraint in PostgreSQL table needed for active bookings. | Enhance `create_booking` RPC with idempotency key, holiday check, outbox event, and add partial unique index in SQL. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/bookingService.js` | Verified (Automated Tests & Build) |
| **2. Overlap-Detection** | Yes | `create_booking` in `08_transactional_booking_rpcs.sql` | Yes | Relied on exact slot match instead of slot time range overlap comparison `new_start < existing_end AND new_end > existing_start` for custom slots. | Add explicit slot time overlap validation in SQL RPC and partial unique constraint per student. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **3. Idempotency** | Partial | Client-side ref checks in `FindSeat.jsx` | No | RPC functions (`create_booking`, `cancel_booking`, `join_waitlist`, `process_checkin`, `process_checkout`) lacked database-level idempotency table. | Create `idempotency_keys` table and integrate `p_idempotency_key` into all critical RPC transactions. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/` | Verified |
| **4. Seat-Availability** | Yes | `occupancyService.js`, `bookingService.js` | Partial | Availability computed in frontend JS; needed authoritative PostgreSQL function evaluating physical status, maintenance, admin block, slot cancellation, active bookings, waitlist holds, and closures. | Implement `get_seat_availability` SQL RPC function and connect student & librarian dashboards. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/occupancyService.js` | Verified |
| **5. FIFO Waitlist** | Yes | `09_waitlist_rpcs.sql` (`join_waitlist`, `get_student_waitlist_position`) | Yes | Needed verification of `joined_at ASC, id ASC` index, restriction check, and position calculation without leaking private student data. | Enforce deterministic ordering `joined_at ASC, id ASC`, composite index, and secure position query. | `supabase/migrations/09_waitlist_rpcs.sql`, `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **6. Waitlist-Promotion** | Yes | `09_waitlist_rpcs.sql`, `waitlistService.js` | Yes | Needed `FOR UPDATE SKIP LOCKED` for concurrent worker safety, seat hold creation (`held` state), expiring offer timestamp (`offered_until`), and outbox record. | Update `process_waitlist_promotion` RPC with `FOR UPDATE SKIP LOCKED`, 15-min offer expiry, outbox event, and concurrency guards. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **7. Waitlist Offer Accept & Expiry** | Partial | `waitlistService.js`, `09_waitlist_rpcs.sql` | RPC & Worker | Acceptance & rejection RPCs lacked idempotency key; batch cron worker for expired offers needed `FOR UPDATE SKIP LOCKED` batching. | Add `accept_waitlist_offer`, `reject_waitlist_offer`, `expire_waitlist_offers_batch` RPCs and tie to background worker. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/waitlistService.js` | Verified |
| **8. Booking Cancellation** | Yes | `08_transactional_booking_rpcs.sql` (`cancel_booking`) | Yes | Role-based actor permissions (student vs librarian vs admin) needed strict enforcement; notification outbox entry missing. | Update `cancel_booking` RPC to enforce actor role permissions, outbox notifications, waitlist promotion trigger, and audit logging. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **9. Check-in & No-Show** | Yes | `10_checkin_checkout_rpcs.sql`, `13_automation_cron_jobs.sql` | Yes | `process_no_shows` needed `FOR UPDATE SKIP LOCKED` batching, idempotency log, and sliding window penalty update trigger. | Enhance `process_no_shows` RPC with row locking, outbox event, and restriction record updates. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **10. Checkout & Auto-Completion** | Yes | `10_checkin_checkout_rpcs.sql` | Yes | Needed batch `FOR UPDATE SKIP LOCKED` for auto-completion at slot end and outbox events. | Update `auto_complete_ended_slots` RPC with batch locking and idempotency. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **11. Repeated No-Show Restriction** | Partial | `PenaltiesRestrictionsPage.jsx`, `adminService.js` | PostgreSQL | Scan was done on historical table in frontend; needed indexed recent-event query or `user_restrictions` table with sliding window evaluation. | Create `user_restrictions` table and `check_user_restriction_status()` PostgreSQL function with index. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **12. Secure QR** | Partial | `QRScannerPage.jsx`, `10_checkin_checkout_rpcs.sql` | Edge Function / RPC | QR code token in `bookings` was a plain UUID without HMAC signature or nonce replay prevention. | Create Supabase Edge Function `verify-qr-pass` with HMAC-SHA256 signature verification and nonce tracking table `scan_nonces`. | `supabase/functions/verify-qr-pass/index.ts`, `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **13. Slot-Disable & Closure** | Yes | `DisableSlotModal.jsx`, `11_admin_ops_rpcs.sql` | Yes | Needed to set `cancelled_by_library` status on affected bookings & waitlist, expire holds, insert outbox notifications, and update UI. | Enhance `emergency_disable_slot` and `emergency_close_library` RPCs with `cancelled_by_library` status handling and outbox notifications. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **14. Seat Maintenance** | Yes | `SeatMaintenancePage.jsx`, `11_admin_ops_rpcs.sql` | Yes | Needed physical condition separation from reservation availability and real-time updates across all dashboards. | Enhance `set_seat_maintenance` RPC and ensure Realtime subscriptions on `seat_maintenance` table trigger UI updates. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/occupancyService.js` | Verified |
| **15. Realtime Synchronization** | Yes | `useSync.js`, `LiveOccupancyPage.jsx` | Yes | Unfiltered table subscriptions on some pages; duplicate channel creation on rerender. | Filter Realtime subscriptions per user / room / library; add reconnect state refetching and clean teardown. | `src/hooks/useSync.js`, `src/features/` | Verified |
| **16. Notification Outbox** | Partial | Direct inserts into `notifications` table | `notification_outbox` | Notifications inserted directly during RPC without outbox retry, backoff, or failure tracking. | Create `notification_outbox` table, update RPCs to write to outbox, and create `process_notification_outbox` worker RPC. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **17. Live Occupancy** | Yes | `occupancyService.js`, `LiveOccupancyPage.jsx` | Yes | Formula needs explicit `(checked-in operational seats / total operational seats) * 100`, color coding (Green 0-59%, Amber 60-84%, Red 85-100%). | Update `occupancyService.js` and `LiveOccupancyPage.jsx` with color thresholds and indexed active booking query. | `src/services/occupancyService.js`, `src/features/librarian/LiveOccupancyPage.jsx` | Verified |
| **18. Seat Recommendation** | Partial | `FindSeat.jsx` | Student Dashboard | Recommendation ranking logic was simple filter; needed weighted score ranking (facility match, preferred zone, power outlet, accessibility). | Implement `getRecommendedSeats(seats, userPreferences)` weighted ranking algorithm in `bookingService.js` / `FindSeat.jsx`. | `src/services/bookingService.js`, `src/features/student/FindSeat.jsx` | Verified |
| **19. Search & Filtering** | Yes | `StudentManagementPage.jsx`, `StaffManagementPage.jsx` | Yes | Some pages filtered entire client-side arrays; missing composite indexes on `(student_id, booking_date)`, `(seat_id, slot_id, status)`. | Add composite indexes in SQL migration and debounced server-side search helpers. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/` | Verified |
| **20. Keyset Pagination** | Partial | Offset-based pagination in `AuditLogsPage.jsx` | Services & RPCs | Offset pagination used on large tables (`bookings`, `activity_logs`, `audit_logs`). | Add keyset/cursor pagination (`created_at DESC, id DESC`) helper functions in `bookingService.js` and `adminService.js`. | `src/services/bookingService.js`, `src/services/adminService.js` | Verified |
| **21. Rate Limiting** | No | Missing | RPC / Edge Function | No rate limiting on booking attempts, waitlist joins, or QR scans. | Create `user_rate_limits` table and `check_rate_limit(p_user_id, p_action, p_max_requests, p_window_seconds)` RPC function in SQL. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **22. Cache & Invalidation** | Partial | Memory cache in `occupancyService.js` | Service layer | Invalidation was manual; needed clear cache invalidation triggered on Supabase Realtime events. | Create cache-ready service wrappers in `occupancyService.js` and `slotService.js` with Realtime invalidation triggers. | `src/services/occupancyService.js`, `src/services/slotService.js` | Verified |
| **23. Audit Log** | Yes | `activity_logs` / `audit_logs` table | Yes | Append-only RLS policy needed verification so non-admins cannot mutate or delete audit logs. | Add strict RLS policy preventing UPDATE/DELETE on `audit_logs` and `activity_logs` by standard users. | `supabase/migrations/17_algorithm_enhancements.sql` | Verified |
| **24. Analytics** | Yes | `ReportsAnalyticsPage.jsx`, `adminService.js` | Yes | Calculated in client JavaScript; needed database view or aggregate query for occupancy rate, completion rate, no-show rate, waitlist conversion rate. | Create SQL analytics RPC `get_system_analytics_summary()` and connect to Admin Analytics page. | `supabase/migrations/17_algorithm_enhancements.sql`, `src/services/adminService.js` | Verified |
| **25. Demand Forecasting & Anomaly Detection** | Partial | Heuristic charts in `ReportsAnalyticsPage.jsx` | Admin Analytics service | Lacked exponential moving average (EMA) demand forecasting and rule-based anomaly detection for booking abuse / QR failures. | Implement EMA demand forecasting and rule-based anomaly detection functions in `adminService.js` and render on Admin Analytics dashboard. | `src/services/adminService.js`, `src/features/admin/ReportsAnalyticsPage.jsx` | Verified |
